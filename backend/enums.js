const ElevatorDirection = {
    UP: "up",
    DOWN: "down",
    IDLE: "idle",
};

const ElevatorState = {
    IDLE: "idle",
    MOVING: "moving",
}

module.exports = {
    ElevatorDirection, 
    ElevatorState,
}
