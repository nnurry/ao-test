const ElevatorDirection = {
    UP: "up",
    DOWN: "down",
    IDLE: "idle",
};

const ElevatorState = {
    IDLE: "idle",
    MOVING: "moving",
    LOADING: "loading",
}

module.exports = {
    ElevatorDirection, 
    ElevatorState,
}
